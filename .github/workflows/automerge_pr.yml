name: automerge
on:
  check_suite:
    types:
      - completed
jobs:
  automerge:
    runs-on: ubuntu-22.04
    steps:
      - id: automerge
        name: automerge
        uses: pascalgn/automerge-action@v0.16.2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}